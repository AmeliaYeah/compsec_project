# Secure Web Application

To run the web application, run the `./run_server.sh` script in the root directory of this github repository.

As in, you should have the `compsec_project` subdirectory visible, containing all the python source code.