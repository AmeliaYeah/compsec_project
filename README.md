# Secure Web Application

To run the web application, make sure you are in the root directory of the github repository. You should see the `compsec_project` package (with all the python scripts inside).

From here, to execute the app, run `python3 -m compsec_project.app`

You should see the server spin up.

It will generate an admin user with a password if the database has not been initialized already.